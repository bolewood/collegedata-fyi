# Python (extraction worker)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Node / Deno (Supabase edge functions)
node_modules/
.deno/

# Supabase local dev
.supabase/
supabase/.env
.env
.env.local

# Extraction workspace — raw data lives in Supabase Storage, never in git
/tmp/
*.pdf
*.docling.json
raw-extracts/
extracted/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*.code-workspace

# Local finder run logs
tools/finder/*.log

# IPEDS HD download cache (re-fetched on demand)
tools/finder/.ipeds-cache/

# Claude Code local state
# (remove this line if you want to commit shared Claude Code project settings)
.claude/

# Personal planning — never commit
/notes/
/scratch/
tools/tier_probe/.venv/
tools/extraction_worker/.venv/

# Operational run logs — too noisy for the repo
drain-*.log

# Playwright MCP local browser profile / cache
.playwright-mcp/
.vercel
.env*.local
